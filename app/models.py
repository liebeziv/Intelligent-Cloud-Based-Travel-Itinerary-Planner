"""Deprecated models stub.

The project now uses DynamoDB for persistence. The legacy SQLAlchemy
models are retained in `app/models_sqlalchemy.py` for reference only.
Importing `app.models` will raise to avoid accidental runtime usage.
"""

raise ImportError("The SQLAlchemy models are deprecated. See app/models_sqlalchemy.py for reference.")

